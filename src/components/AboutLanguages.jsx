import React from "react";

const AboutLanguage = props => (
  <div className="container about-languages end-line">
    <h2 className="about-header">Languages ..</h2>
    <p>{props.languages}</p>
  </div>
);

export default AboutLanguage;
